import * as React from 'react';
import styled from 'styled-components';

const Column = styled.div`
    flex: none;
    white-space: nowrap;
    display: flex;
    flex-direction: column;
    min-height: 100%;
`;

export const TimeGutter: React.FC = () => <TimeGutter></TimeGutter>;
