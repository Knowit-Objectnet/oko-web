// The role string values below must match the user role names defined in the Keycloak server
export enum Roles {
    Admin = 'reg_employee',
    Partner = 'partner',
    Stasjon = 'reuse_station',
}
