---
name: Time Slot Group
menu: Calendar
---

import { Playground, Props } from 'docz';
import { TimeSlotGroup } from '../../src/sharedComponents/Calendar/TimeSlotGroup';

# Calendar - Time Slot Group

This is a single time slot group, it contains two time slots. It does nothing but assign id's to it's two slots.

## Props

<Props of={TimeSlotGroup} />
