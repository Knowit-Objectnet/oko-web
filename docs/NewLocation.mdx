---
name: NewLocation
---

import { Playground, Props } from 'docz';
import useModal from '../src/sharedComponents/Modal/useModal';
import { NewLocation } from '../src/sharedComponents/NewLocation/NewLocation';

# NewLocation
## File: NewLocation.tsx

## Basic usage

<Playground>
  {() => {
    const modal = useModal();
    return (
      <button onClick={() =>
            modal.show(<NewLocation />)
        }
      >
        Click me!
      </button>
    )
  }}
</Playground>