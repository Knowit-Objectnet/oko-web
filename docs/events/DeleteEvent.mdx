---
name: Event - Delete event
menu: Events
---

import { Playground, Props } from 'docz';
import { DeleteEvent } from '../../src/sharedComponents/Events/DeleteEvent';
import styled from 'styled-components';

# Event - Delete event
## File: DeleteEvent.tsx

Component used inside the Event component to allow for the deletion of an event or a range of events.

## Props

<Props of={DeleteEvent} />

## Showcase

<Playground>
  {() => {
      const Wrapper = styled.div`
        width: 100%;
        margin-bottom: 150px;
        position: relative;
      `;
    return (
        <Wrapper>
            <div>
                <DeleteEvent onSubmit={() => {/* linter be quite */}} />
            </div>
        </Wrapper>
    )
  }}
</Playground>
