---
name: Event - View Event
menu: Events
---

import { Playground, Props } from 'docz';
import useModal from '../../src/sharedComponents/Modal/useModal';
import { Event } from '../../src/sharedComponents/Events/Event';

# Event - View Event
## File: Event.tsx

Component used in a Modal to show an Event. It's build from the horizontal template and options.

## Props

<Props of={Event} />

## Usage

<Playground>
  {() => {
    const modal = useModal();
    const showEvent = () => {
        modal.show(
            <Event
                title="Test title"
                start={new Date()}
                end={new Date()}
                resource={{
                  eventId: 1,
                  location: {
                      id: 2,
                      name: "Grønmo",
                      hours: {
                          MONDAY: ["07:00:00Z", "20:00:00Z"],
                          TUESDAY: ["07:00:00Z", "20:00:00Z"],
                          WEDNESDAY: ["07:00:00Z", "20:00:00Z"],
                          THURSDAY: ["07:00:00Z", "20:00:00Z"],
                          FRIDAY: ["07:00:00Z", "20:00:00Z"],
                      },
                  },
                  partner: {
                      id: 1,
                      name: 'Fretex',
                      description: "...",
                      phone: "40400040",
                      email: "test@knowit.no",
                  },
                  weight: 100,
                  message: {
                      start: new Date(),
                      end: new Date(),
                      text: 'Tar ikke i mot barneleker ifm. Covid-19 tiltak.',
                  },
                  recurrenceRule: null
                }}
            />
        )
    }
    return (
        <button onClick={() => showEvent()}>Show event</button>
    )
  }}
</Playground>
