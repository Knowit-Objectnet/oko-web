---
name: Message Box
menu: Events
---

import { Playground, Props } from 'docz';
import { EventMessageBox } from '../../src/pages/calendar/events/EventMessageBox';

# Message Box

## Props

<Props of={EventMessageBox} />

## No message and date range given

<Playground>
  {() => {
    return (
      <EventMessageBox />
    )
  }}
</Playground>

## Message given, but no date range

<Playground>
  {() => {
    return (
      <EventMessageBox text="Leker hentes ikke pga. korone-virus" />
    )
  }}
</Playground>

## Message and date range given

<Playground>
  {() => {
    return (
      <EventMessageBox text="Leker hentes ikke pga. korone-virus" start={new Date()} end={new Date()} />
    )
  }}
</Playground>
