---
name: DeleteLocation
---

import { Playground, Props } from 'docz';
import useModal from '../src/sharedComponents/Modal/useModal';
import { DeleteLocation } from '../src/sharedComponents/DeleteLocation';

# DeleteLocation
## File: DeleteLocation.tsx

## Basic usage

<Playground>
  {() => {
    const modal = useModal();
    return (
      <button onClick={() =>
            modal.show(<DeleteLocation />)
        }
      >
        Click me!
      </button>
    )
  }}
</Playground>