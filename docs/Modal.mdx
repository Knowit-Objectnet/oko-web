---
name: Modal
---

import { Playground, Props } from 'docz';
import useModal from '../src/sharedComponents/Modal/useModal';
import styled from 'styled-components';

# Modal

## File: useModal.tsx

## Options

Options can be found in '../src/sharedComponents/Modal/Types.ts';

## Basic usage

<Playground>
  {() => {
    const modal = useModal();
    return (
      <button onClick={() =>
            modal.show(<div>
                Hello world
                <button onClick={() => modal.remove()}>Close the modal</button></div>,
                {width: "200px", height: "200px"}
            )
        }
      >
        Click me!
      </button>
    )
  }}
</Playground>
